<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FindU" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textColor">#111111</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/gray6</item>
        <item name="android:logo">@mipmap/ic_launcher_round</item>
        <item name="displayOptions">useLogo</item>
    </style>


    <style name="Theme.FindU" parent="Base.Theme.FindU" />
</resources>